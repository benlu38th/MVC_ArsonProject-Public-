@model MVC_ArsonProject.Models.Knowledge

@{
    ViewBag.Title = "Delete";
}

<h2>知識庫管理</h2>

<h3>你確定要刪除這個嗎??</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DownloadFileUrl)
        </dt>

        <dd>
            <a href="@Model.DownloadFileUrl" download>下載</a>
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.InitDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.InitDate)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="確認刪除" class="btn app-btn-danger" />
            <a href="@Url.Action("Index")" class="btn app-btn-primary">返回</a>
        </div>
    }
</div>
