@model MVC_ArsonProject.Models.User

@{
    ViewBag.Title = "Details";
}

<h2>帳號權限設定</h2>

<div>
    <h4>細節</h4>
    <p>
        <a href="@Url.Action("PermissionEdit", "Users", new { id = Model.Id })" class="btn app-btn-primary">編輯</a>
        <a href="@Url.Action("Index", "Users")" class="btn app-btn-primary">返回</a>
    </p>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Account)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Account)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.role)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.role)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Permission)
        </dt>

        <dd>
            @ViewBag.UserPermissions
        </dd>
    </dl>
</div>
